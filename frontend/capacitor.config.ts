import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abca.jo',
  appName: 'تحدي الطرنيب',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
